# Stats.app Settings Sync

Sync [Stats](https://github.com/exelban/stats) preferences between Macs.

```bash
# Export current settings
./sync.sh export

# Import on another machine
./sync.sh import
```

The exported `Stats.plist` file is saved alongside the script. Commit it to your dotfiles.
